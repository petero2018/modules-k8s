apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: ${name}
  namespace: ${namespace}
spec:
  gateways:
  - ${gateway}
  hosts:
  - ${hostname}
  http:
  - route:
    - destination:
        host: ${destination}
