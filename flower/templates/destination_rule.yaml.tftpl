apiVersion: networking.istio.io/v1beta1
kind: DestinationRule
metadata:
  name: "${name}"
  namespace: "${namespace}"
spec:
  host: ${name}.${namespace}.svc.cluster.local
  trafficPolicy:
    loadBalancer:
      consistentHash:
        httpCookie:
          name: visitorId
          ttl: 0s
